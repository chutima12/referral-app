# ── คัดลอกไฟล์นี้เป็น .env.local (ตอนรันในเครื่อง) และตั้งค่าใน Vercel (ตอนดีพลอย) ──

# จาก Supabase → Project Settings → API
SUPABASE_URL=https://xxxxxxxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOi...(service_role key — เก็บเป็นความลับ ห้ามเผยแพร่)

# รหัสผ่านสำหรับหน้าแอดมิน (ตั้งเองได้เลย)
ADMIN_PASSWORD=lfl-admin-2569
